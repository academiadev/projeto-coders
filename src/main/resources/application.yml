app:
  name: reembolsocoders
spring:
  application:
    name: reembolsocoders
  datasource:
    url: jdbc:postgresql://localhost/reembolso
    username: postgres
    password: 123
  jpa:
    generate-ddl: true
server:
  port: ${PORT:8080}
jwt:
  header: ${HEADER:Authentication}
  expires_in: ${EXPIRES_IN:3000}
  mobile_expires_in: ${EXPIRES_IN_MOBILE:3000}
  secret: ${SECRET:sredoc}
management:
  security:
    enable: false